---
description: DashboardPage 測試案例
---

> 狀態：初始為 [ ]、完成為 [x]
> 注意：狀態只能在測試通過後由流程更新。
> 測試類型：前端元素、function 邏輯、Mock API、驗證權限...

---

## [x] 【前端元素】渲染儀表板基本元件
**範例輸入**：渲染 `DashboardPage` 元件  
**期待輸出**：顯示「儀表板」標題、歡迎區塊、登出按鈕

---

## [x] 【前端元素】管理員角色顯示管理後台連結
**範例輸入**：`user.role` 為 'admin' 時渲染 `DashboardPage`  
**期待輸出**：畫面上顯示「🛠️ 管理後台」連結

---

## [x] 【前端元素】一般用戶不顯示管理後台連結
**範例輸入**：`user.role` 為 'user' 時渲染 `DashboardPage`  
**期待輸出**：畫面上不顯示「🛠️ 管理後台」連結

---

## [x] 【function 邏輯】點擊登出按鈕
**範例輸入**：點擊「登出」按鈕  
**期待輸出**：呼叫 `logout()`，並呼叫 `navigate('/login', { replace: true, state: null })` 導向登入頁

---

## [x] 【Mock API】顯示商品載入中狀態
**範例輸入**： API 請求尚未完成時 (`isLoading` 為 true)  
**期待輸出**：顯示「載入商品中...」與 Loading 提示

---

## [x] 【Mock API】成功獲取並渲染商品列表
**範例輸入**：模擬 `productApi.getProducts()` 成功回傳商品陣列資料  
**期待輸出**：畫面不顯示 Loading，並正確渲染出對應的商品卡片 (商品名稱、描述、價格)

---

## [ ] 【Mock API】獲取商品失敗顯示錯誤訊息
**範例輸入**：模擬 API 回傳非 401 錯誤（例如回傳 500 與 message: "伺服器錯誤"）  
**期待輸出**：畫面顯示帶有 ⚠️ 圖示的錯誤訊息 "伺服器錯誤"
